# Build output
.build/
.build-arm64/
.build-x86_64/
.dmg-stage/
*.app/
*.dmg
.DS_Store

# Raw screenshot exports at the repo root; canonical PNGs live in docs/screenshots/
# (anchored with a leading slash: an unanchored pattern also matches docs/screenshots
# on case-insensitive filesystems)
/Screenshots/
