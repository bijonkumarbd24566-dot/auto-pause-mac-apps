# Screenshots

Images referenced by the main [README](../../README.md). Captured on a 16 GB M1 MacBook Pro.

| File | Shows |
|---|---|
| `main-panel.png` | The menu bar panel: suspended apps pinned on top, running apps sorted by resident RAM with sparklines, and the Background Services section |
| `free-up-memory.png` | The Free Up Memory dialog — GB target slider and availability estimate |
| `system-dashboard.png` | Memory pressure ring, usage history, and the App/Wired/Compressed/Free/Swap breakdown |
| `app-detail.png` | Per-app detail popover with memory history and the auto-pause toggle |
| `frozen-app.png` | A frozen app's detail view, showing reclaimed memory and the Resume action |

To retake them: open the menu bar panel and use `⌘⇧4` then `Space` to capture the window.
